enum NotifierState {
  ERROR,
  SUCCESS,
  SUCCESS_EMPTY,
  ERROR_NOT_LOGGED_IN,
  SUCCESS_NOTHING_TO_UPDATE,
}

enum RCType {
  STRING,
  INT,
  DOUBLE,
  BOOL
}

enum StorageFileType{
  IMAGE_PNG,
  IMAGE_JPG,
}